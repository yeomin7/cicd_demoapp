build:
    build_starting:
        image: python:2
        commands:
            - echo "Beginning new build"
    run_tests:
        image: python:2-alpine
        commands:
            - pip install -r requirements.txt
            - python testing.py

publish:
    docker:
        repo: yeomin7/cicd_demoapp
        tag: latest
        username: yeomin
        password: 0131qpqp
        email: yeomin@cisco.com 
